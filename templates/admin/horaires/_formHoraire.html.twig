{{ form_start(form) }}
<div class="row">
  <div class="col-md-6 text-center">{{ form_row(form.jour) }}</div>
</div>
<div class="row">
  <div class="col-md-3 text-center">{{ form_row(form.ouvertureMatin) }}</div>
  <div class="col-md-3 text-center">{{ form_row(form.fermetureMatin) }}</div>
</div>
<div class="row">
  <div class="col-md-3 text-center">{{ form_row(form.ouvertureApresMidi) }}</div>
  <div class="col-md-3 text-center">{{ form_row(form.fermetureApresMidi) }}</div>
</div>
{{ form_widget(form) }}
<button class="btn btn-primary">{{ button|default('Enregistrer') }}</button>
{{ form_end(form) }}
